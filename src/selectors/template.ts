import { TagWithAttributes } from '../types';

const TemplateButtons: TagWithAttributes[] = [
	{
		tag: 'span',
		attributes: [{ attribute: 'class', value: 'gamepad-2nd-icon-wrapper' }],
	},
];

export default TemplateButtons;
