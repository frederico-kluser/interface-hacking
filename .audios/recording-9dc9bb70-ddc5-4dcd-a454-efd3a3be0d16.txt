Atualmente nós temos o arquivo dentro de src chamado index.ts e nós temos funções helpers que servem para esperar elementos aparecerem, para encontrar um elemento ou encontrar elementos, tá? E eu tenho uma estrutura que está no formato de template dentro da pasta selectors, que ela demonstra como a gente cria seletores para serem usados juntos com essa função de maneira profissional. O que acontece é que no nosso arquivo index.ts, dentro da pasta src, nós estamos usando capturas de elemento, porém com document carry selector, all document carry selector e não estamos usando as nossas funções que estão dentro da pasta src, do find element by hierarchy, find elements by hierarchy e o wait element by hierarchy. E o que acontece é que eu quero substituir as funções que estão no index.ts para usar as nossas funções de encontrar o elemento pela hierarquia, tá? Se for mais de um é a função elements, perdão, é o arquivo elements ali do src, né? O find by hierarchy. Se for apenas um item é o find element by hierarchy. Só que eu não quero só usar essas funções dentro do index.ts, eu quero usar elas, só que os seletores têm que estar dentro da pasta selectors, seguindo o padrão do template.ts.
